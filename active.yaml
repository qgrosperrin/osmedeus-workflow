name: active
desc: Running "active" reconnaissance activities
type: general
validator: domain

usage: |
    osmedeus scan -f active -t target.com

routines:
  - modules:
      - ntlm
      - dirbscan
  - modules:
      - spider
  - modules:
      - js
      - secrets
  - modules:
      - vulnscan
      - portscan
  - modules:
      - summary

